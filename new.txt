1 request的属性
		2 render,redirect
		3 深度查询    句点符号  .
		4 过滤器  {{变量|filter_name:参数}}
		   date
		   filesizeformat
		   safe
		   default
		5  for 标签
		       {% for i in [] %}
			   {% endfor%}
		  
		       {{forloop.last}}
		       {{forloop.counter}}
		6.新添加的内容：
		（1）开发到了一半 出现了bug
		（2）添加了新功能，
                 (3)再次开发